[phases.setup]
nixPkgs = ["python311", "ffmpeg"]
PYTHONPATH = "."

[phases.install]
cmds = [
  "ln -s $(which python3) /usr/local/bin/python",
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install -r requirements.txt"
]

[start]
cmd = "python3 -m tg_bot.main"
